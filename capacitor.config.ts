import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.qrsof.slird',
  appName: 'slird-landing',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
